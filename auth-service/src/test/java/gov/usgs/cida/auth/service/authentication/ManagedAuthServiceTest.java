package gov.usgs.cida.auth.service.authentication;

public class ManagedAuthServiceTest {
	//TODO, this test class should use a mock server to mimic an Attlassian Crowd auth endpoint which will
	//1) authenticate valid users
	//2) reject bad users
	//3) ensure roles are set
}
